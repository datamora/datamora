apps:
  root: 
    ref: apps.index:app
  mounts:
    -
      ref: 'apps.fois'
      path: /streams
      inject:
          datastore: $datastore:master
    -
      ref: apps.config_debugger
      path: /debug
      config:
          scan: data